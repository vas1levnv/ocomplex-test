export interface ProductsInterface {
    id: string;
    image_url: string;
    description: string;
    price: number;
    title: string;
}

export interface ReviewsItem {
    id: string;
    text: string;
}
